{
    "name": "customs-ruling-finder",
    "version": "0.1.0",
    "type": "module",
    "description": "CBP classification precedent from the keyless CROSS API: search customs rulings by product name or tariff code, get the HTS codes Customs assigned, the full ruling text, and a precedent status that flags revoked and modified rulings. Handles the code format mismatch between CROSS and the tariff schedule. No browser, no login, no API key.",
    "main": "src/main.js",
    "scripts": {
        "start": "node src/main.js",
        "test": "node test/parse.test.mjs"
    },
    "dependencies": {
        "apify": "^3.7.0"
    },
    "engines": {
        "node": ">=20"
    }
}
