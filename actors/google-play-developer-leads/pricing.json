{
    "_note": "PUT this to /v2/acts/{actorId} as a pricingInfos array entry. PAY_PER_EVENT only on BRONZE. startedAt must be 14+ days out. Apify auto-adds 20% margin. This actor is plain HTTP (CheerioCrawler, no browser), so compute per lead is tiny and these prices stay well above it. High volume, so prices are set low. First 10 qualified_lead per run are free for validation. | 2026-08-17 pass: per-run free allowance cut to 1 (0 where average output is under ~2 rows/run). It is charged per run, not per user, so on low-yield actors it was zeroing out revenue. Descriptions corrected to match src/main.js. Effective 09-01.",
    "_endpoint": "PUT https://api.apify.com/v2/acts/scrapemint~google-play-developer-leads?token=APIFY_TOKEN",
    "pricingModel": "PAY_PER_EVENT",
    "pricingPerEvent": {
        "actorChargeEvents": {
            "listing": {
                "eventTitle": "App listing",
                "eventDescription": "One app with developer name, category, install band, rating, and monetization flags, but no public developer email.",
                "eventPriceUsd": 0.01
            },
            "lead": {
                "eventTitle": "Developer lead",
                "eventDescription": "One app whose listing publishes a developer contact email, plus developer name, website where available, category, installs, and rating.",
                "eventPriceUsd": 0.02
            },
            "qualified_lead": {
                "eventTitle": "Qualified developer lead",
                "eventDescription": "An app with a developer contact email, a developer website, and installs at or above the qualified bar. A reachable, established developer. The first qualified lead per run is free so you can validate output.",
                "eventPriceUsd": 0.05
            }
        }
    },
    "startedAt": "2026-09-01T00:00:00.000Z"
}
