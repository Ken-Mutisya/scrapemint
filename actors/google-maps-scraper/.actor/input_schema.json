{
    "title": "Google Maps Intelligence Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "searchQueries": {
            "title": "Search queries",
            "type": "array",
            "description": "Queries to run on Google Maps. Examples: \"coffee shops in Austin\", \"dentist 78704\", \"plumbers near Miami\".",
            "editor": "stringList",
            "prefill": [
                "coffee shops in Austin TX"
            ],
            "default": [
                "coffee shops in Austin TX"
            ]
        },
        "startUrls": {
            "title": "Direct place URLs",
            "type": "array",
            "description": "Direct Google Maps place URLs to scrape without running a search. Example: https://www.google.com/maps/place/.../@...",
            "editor": "stringList",
            "default": []
        },
        "language": {
            "title": "Language code",
            "type": "string",
            "description": "Google Maps UI language. Affects review translation and address format.",
            "default": "en",
            "editor": "textfield"
        },
        "maxPlacesPerQuery": {
            "title": "Max places per query",
            "type": "integer",
            "description": "Cap on places returned per search query.",
            "default": 10,
            "minimum": 1,
            "maximum": 500
        },
        "maxPlacesTotal": {
            "title": "Max places per run",
            "type": "integer",
            "description": "Hard cap on places extracted per run. Controls total cost.",
            "default": 20,
            "minimum": 1,
            "maximum": 2000
        },
        "scrapeReviews": {
            "title": "Scrape reviews",
            "type": "boolean",
            "description": "Open each place and scroll the reviews panel. Off by default because it roughly triples run time (38.5s vs 10.7s measured on the same query) while the actor bills per place, not per review. Turn it on when you need review text.",
            "default": false
        },
        "maxReviewsPerPlace": {
            "title": "Max reviews per place",
            "type": "integer",
            "description": "Reviews per place cap. 0 skips reviews.",
            "default": 5,
            "minimum": 0,
            "maximum": 500
        },
        "scrapeImages": {
            "title": "Scrape images",
            "type": "boolean",
            "description": "Extract image URLs from each place.",
            "default": false
        },
        "maxImagesPerPlace": {
            "title": "Max images per place",
            "type": "integer",
            "description": "Image URLs per place cap.",
            "default": 5,
            "minimum": 0,
            "maximum": 50
        },
        "enrichFromWebsite": {
            "title": "Enrich from business website",
            "type": "boolean",
            "description": "Visit the business website and pull emails and person names with job titles. Adds 5 to 10 seconds per place.",
            "default": false
        },
        "dedupe": {
            "title": "Deduplicate across runs",
            "type": "boolean",
            "description": "Skip place IDs returned by your previous runs. Off by default: with it on, a repeat run on the same query can return zero places (measured 0 rows vs 6 for the identical query with it off), which looks like a broken run. Turn it on when you only want places you have not seen before, and expect empty runs.",
            "default": false
        },
        "proxyConfiguration": {
            "title": "Proxy configuration",
            "type": "object",
            "description": "Apify proxy. Residential strongly recommended. Google blocks datacenter IPs.",
            "editor": "proxy",
            "prefill": {
                "useApifyProxy": true,
                "apifyProxyGroups": [
                    "RESIDENTIAL"
                ]
            },
            "default": {
                "useApifyProxy": true,
                "apifyProxyGroups": [
                    "RESIDENTIAL"
                ]
            }
        }
    },
    "required": []
}
