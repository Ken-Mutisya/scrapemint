# Retired actor

This actor has been retired. LinkedIn now login-walls anonymous access to company people pages, so a cookieless scraper cannot return employee data.

Runs exit immediately, return a single notice row, and are never charged. The actor will be removed from the Apify Store shortly.
