# Retired actor

This actor has been retired. LinkedIn now login-walls anonymous access to member activity feeds, so a cookieless scraper cannot return post data.

Runs exit immediately, return a single notice row, and are never charged. The actor will be removed from the Apify Store shortly.
