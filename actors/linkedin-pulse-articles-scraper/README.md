# Retired actor

This actor has been retired. LinkedIn now requires authentication to read Pulse articles, so a cookieless scraper cannot return article content.

Runs exit immediately, return a single notice row, and are never charged. The actor will be removed from the Apify Store shortly.
