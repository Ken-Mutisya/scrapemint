{
    "name": "youtube-video-scraper",
    "version": "0.1.0",
    "type": "module",
    "description": "YouTube search results, channel video listings and video details as clean rows, parsed from YouTube's server-rendered data. Views, dates, durations, thumbnails and channel stats. No browser, no API key.",
    "main": "src/main.js",
    "scripts": {
        "start": "node src/main.js"
    },
    "dependencies": {
        "apify": "^3.7.0"
    },
    "engines": {
        "node": ">=20"
    }
}
