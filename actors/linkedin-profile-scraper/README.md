# Retired actor

This actor has been retired. LinkedIn now login-walls anonymous access to member profile pages, so a cookieless scraper cannot return profile data.

Runs exit immediately, return a single notice row, and are never charged. The actor will be removed from the Apify Store shortly.
