{
    "name": "federal-contract-momentum-pipeline",
    "version": "0.1.0",
    "type": "module",
    "description": "Convergence pipeline. For each company, joins accelerating federal contract awards (USAspending) with insider open-market buying (SEC Form 4), scores 0-100, and tiers companies converging / contract_only / insider_only / watch. Pure HTTP and public APIs, no browser, no API keys.",
    "main": "src/main.js",
    "scripts": {
        "start": "node src/main.js"
    },
    "dependencies": {
        "apify": "^3.7.0"
    },
    "engines": {
        "node": ">=20"
    }
}
