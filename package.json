{
  "name": "scrapemint",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "workspaces": [
    "actors/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check automation/*.mjs 2>&1 || true",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "dependencies": {
    "@crawlee/basic": "^3.16.0",
    "@sapphire/shapeshift": "^4.0.0",
    "playwright": "^1.61.1",
    "playwright-core": "^1.61.1"
  }
}
